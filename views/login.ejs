<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700&display=swap" rel="stylesheet">
    <title>Login</title>
    <link rel="stylesheet" href="css/output.css">
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
</head>

<body class="bg-gradient-to-b from-[#0CC0DF] to-[#2563EB] h-screen font-sans">
    <div class="relative h-full w-full flex items-center justify-center">
        <div id="dodofiles" class="absolute top-0 left-0 m-8 text-2xl font-bold text-white">
            <p class="text-5xl">DODO</p>
            <p class="text-5xl">FILES</p>
        </div>

        <div id="signin" class="flex flex-col items-center justify-center">
            <div class="text-white text-4xl font-extrabold mb-4">
                <h2>Sign In</h2>
            </div>
            <div id="signin-form" class="bg-white rounded-lg p-8 shadow-lg w-[42rem]">
                <form hx-post="/sign-in" hx-target="#serverMessage" class="flex flex-col space-y-4">
                    <div class="flex flex-col items-start">
                        <label for="username" class="text-[#7f848d] mb-1">Username</label>
                        <input type="text" name="username" id="username" class="w-full p-2 border rounded-3xl bg-[#D9D9D9]" required/>
                    </div>
                    <div class="flex flex-col items-start">
                        <label for="password" class="text-[#7f848d] mb-1">Password</label>
                        <input type="password" name="password" id="password" class="w-full p-2 border rounded-3xl bg-[#D9D9D9]" required/>
                    </div>
                    <div id="serverMessage" class="serverMessage flex justify-center">
                    </div>
                    <div class="flex justify-center">
                        <button type="submit" class="w-1/4 bg-[#2563EB] text-white px-8 py-4 rounded-2xl hover:bg-blue-500">
                            Login
                        </button>
                    </div>
                    <div class="text-center">
                        <span class="text-[#7f848d]">Don't have an account?  <a class="text-[#2563EB] font-medium" href="/register">Register here</a>.</span>
                      </div>
                </form>
            </div>
        </div>

    </div>
</body>

</html>
